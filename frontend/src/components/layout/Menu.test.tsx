import { Menu } from './Menu';
import { shallow } from "enzyme";

describe("Menu component test suit",()=>{
  
  it(' Render menu ', () => {
    let mountedMenu = shallow(<Menu />);

  });

  it(' Render menu ', () => {
    let mountedMenu = shallow(<Menu />);
    expect(mountedMenu)

  });


})